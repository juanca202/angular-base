import { NavigateBack } from './navigate-back';

describe('NavigateBack', () => {
  it('should create an instance', () => {
    const directive = new NavigateBack();
    expect(directive).toBeTruthy();
  });
});
