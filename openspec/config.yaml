schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
context: |
  Este proyecto es el cliente web, una aplicación que consume servicios REST desde el platform.
  Todas las especificaciones deben estar ajustadas únicamente a lo que concierne a este componente.
  No incluir especificaciones de backend u otros componentes externos.
  
  Tech stack: Angular, TypeScript, Angular Material (MDC), Tailwind CSS
  Arquitectura: Componentes standalone, signals para gestión de estado, estructura modular (Core, Shared, Features)

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
