# Tasks: Administración de Tareas

**Input**: Design documents from `docs/specs/002-task-management/`  
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/  
**Location**: `docs/specs/002-task-management/tasks.md` (per project constitution)

**Tests**: Tests are not explicitly requested in the specification. Focus on implementation tasks.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Path Conventions (Angular Base Project)

- **Angular structure**: `src/app/` with layers: `core/`, `shared/`, `cross/`, `features/`
- **Tests**: `test/` for unit/integration, `e2e/` for E2E tests
- **Feature location**: `src/app/features/tasks/`
- **Layer dependencies**: Features → (Shared, Cross) → Core (see ADR-001)

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [x] T001 Create feature directory structure in `src/app/features/tasks/` with subdirectories: `components/`, `managers/`, `repositories/`, `models/`
- [x] T002 [P] Create component directories: `src/app/features/tasks/components/task-list/`, `src/app/features/tasks/components/task-item/`, `src/app/features/tasks/components/task-form/`
- [x] T003 [P] Create manager directory: `src/app/features/tasks/managers/`
- [x] T004 [P] Create repository directory: `src/app/features/tasks/repositories/`
- [x] T005 [P] Create models directory: `src/app/features/tasks/models/`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T006 [P] Create TaskStatus type in `src/app/features/tasks/models/task-status.ts` with values: 'pending', 'frozen', 'completed'
- [x] T007 [P] Create Task model interface in `src/app/features/tasks/models/task.ts` with all fields from data-model.md (id, title, description, status, dueAt, createdAt, updatedAt, createdBy, updatedBy)
- [x] T008 [P] Create TaskRequestCreate type in `src/app/features/tasks/models/task.ts` (Omit Task fields generated by system)
- [x] T009 [P] Create TaskRequestUpdate type in `src/app/features/tasks/models/task.ts` (Partial update with id)
- [x] T010 [P] Create TaskSearchParams interface in `src/app/features/tasks/models/task.ts` (status?, sort?, order?)
- [x] T011 Create TaskRepository class in `src/app/features/tasks/repositories/task-repository.ts` extending BaseRepository, using MockHttpClient, with baseUrl 'tasks'
- [x] T012 Implement TaskRepository constructor loading mock data from `test/mocks/repositories/tasks.json` using httpClient.loadCollection()
- [x] T013 [P] Update `test/mocks/repositories/tasks.json` with comprehensive mock data: tasks in all states (pending, frozen, completed), with and without dueAt, including overdue tasks

**Checkpoint**: Foundation ready - user story implementation can now begin in parallel

---

## Phase 3: User Story 1 - Visualizar Listado de Tareas por Estado (Priority: P1) 🎯 MVP

**Goal**: El usuario puede visualizar sus tareas organizadas en tres categorías: Pendientes, Congeladas y Completadas. Cada tarea muestra su título e información temporal (tiempo restante o tiempo vencido), con resaltado visual cuando corresponde.

**Independent Test**: Navegar entre las tres pestañas (Pendientes, Congeladas, Completadas) y verificar que las tareas se muestran correctamente con su información temporal y resaltado visual cuando aplica.

### Implementation for User Story 1

- [x] T014 [US1] Implement TaskRepository.findBy() method in `src/app/features/tasks/repositories/task-repository.ts` returning SignalGet<void, Task[]> with status filter parameter
- [x] T015 [US1] Create helper function formatTaskDueDate() in `src/app/features/tasks/models/task.ts` to calculate and format temporal information ("Vence en X días", "Vencida hace X días", etc.)
- [x] T016 [P] [US1] Create TaskItem component in `src/app/features/tasks/components/task-item/task-item.ts` (standalone, OnPush, with @Input() task: Task)
- [x] T017 [P] [US1] Create TaskItem template in `src/app/features/tasks/components/task-item/task-item.html` displaying title and temporal information with visual highlighting for overdue tasks
- [x] T018 [P] [US1] Create TaskItem styles in `src/app/features/tasks/components/task-item/task-item.css` following entity-list patterns
- [x] T019 [US1] Create TaskList component in `src/app/features/tasks/components/task-list/task-list.ts` (standalone, OnPush) with three tabs (Pendientes, Congeladas, Completadas) and signal for current status
- [x] T020 [US1] Implement TaskList template in `src/app/features/tasks/components/task-list/task-list.html` with tabs, task list using @for, and state handling (loading, error, empty) following entity-list pattern
- [x] T021 [US1] Create TaskList styles in `src/app/features/tasks/components/task-list/task-list.css` following wireframes and entity-list patterns
- [x] T022 [US1] Implement TaskList ngOnInit() loading tasks filtered by current status tab using TaskRepository.findBy()
- [x] T023 [US1] Implement tab switching logic in TaskList to reload tasks with new status filter
- [x] T024 [US1] Integrate TaskItem component in TaskList template to display each task
- [x] T025 [US1] Add loading state handling in TaskList template using ft-progress component with "Loading..." message
- [x] T026 [US1] Add error state handling in TaskList template with warning icon, error message, and Retry button
- [x] T027 [US1] Add empty state handling in TaskList template with "No records" message
- [x] T028 [US1] Create tasks-routes.ts in `src/app/features/tasks/tasks-routes.ts` with route to TaskList component
- [x] T029 [US1] Integrate tasks routes in `src/app/app.routes.ts` with lazy loading

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently. Users can navigate between tabs and see tasks organized by status with temporal information.

---

## Phase 4: User Story 2 - Agregar Nueva Tarea (Priority: P1)

**Goal**: El usuario puede crear nuevas tareas mediante un botón "Nueva Tarea" visible en la interfaz. Al crear una tarea, debe proporcionar al menos un título.

**Independent Test**: Hacer clic en el botón "Nueva Tarea", completar el formulario con un título y verificar que la tarea aparece en el listado de Pendientes.

### Implementation for User Story 2

- [x] T030 [US2] Implement TaskRepository.mutations() method in `src/app/features/tasks/repositories/task-repository.ts` with create mutation using POST /tasks endpoint
- [x] T031 [US2] Create TaskManager class in `src/app/features/tasks/managers/task-manager.ts` (Injectable, providedIn: 'root') following EntityManager pattern
- [x] T032 [US2] Implement TaskManager.open() method in `src/app/features/tasks/managers/task-manager.ts` to open TaskForm dialog for creating new task
- [x] T033 [P] [US2] Create TaskForm component in `src/app/features/tasks/components/task-form/task-form.ts` (standalone, OnPush) with reactive form for title (required, max 50 chars) and optional description (max 250 chars) and dueAt
- [x] T034 [P] [US2] Create TaskForm template in `src/app/features/tasks/components/task-form/task-form.html` with form fields following ADR-013 form layout structure
- [x] T035 [P] [US2] Create TaskForm styles in `src/app/features/tasks/components/task-form/task-form.css`
- [x] T036 [US2] Implement TaskForm form validation: title required and max 50 characters, description max 250 characters
- [x] T037 [US2] Implement TaskForm submit handler calling TaskRepository.mutations().create() with TaskRequestCreate
- [x] T038 [US2] Add "Nueva Tarea" button in TaskList header in `src/app/features/tasks/components/task-list/task-list.html` calling TaskManager.open()
- [x] T039 [US2] Implement TaskList effect to reload tasks when TaskRepository change signal is emitted (after create)
- [x] T040 [US2] Ensure new tasks are created with status 'pending' by default

**Checkpoint**: At this point, User Stories 1 AND 2 should both work independently. Users can view tasks and create new ones.

---

## Phase 5: User Story 3 - Seleccionar Tareas Múltiples (Priority: P2)

**Goal**: El usuario puede seleccionar una o varias tareas del listado mediante checkboxes. Cuando hay tareas seleccionadas, la interfaz entra en modo selección y la barra de acciones cambia para mostrar solo acciones relacionadas con la selección (Completar y Eliminar).

**Independent Test**: Seleccionar una o más tareas, verificar que aparece la barra de acciones con botones de Completar y Eliminar, y que el modo selección se activa correctamente.

### Implementation for User Story 3

- [x] T041 [US3] Add selectedTasks signal in TaskList component in `src/app/features/tasks/components/task-list/task-list.ts` (signal<Set<string>>)
- [x] T042 [US3] Add isSelectionMode computed signal in TaskList component based on selectedTasks().size > 0
- [x] T043 [US3] Implement toggleSelection() method in TaskList component to add/remove task IDs from selectedTasks
- [x] T044 [US3] Add checkbox input to TaskItem component in `src/app/features/tasks/components/task-item/task-item.html` with @Input() isSelected and @Output() selectionChange
- [x] T045 [US3] Update TaskItem component in `src/app/features/tasks/components/task-item/task-item.ts` to handle checkbox selection events
- [x] T046 [US3] Integrate TaskItem checkbox in TaskList template binding to selectedTasks signal and toggleSelection method
- [x] T047 [US3] Implement conditional action bar in TaskList template: show "Completar" and "Eliminar" buttons when isSelectionMode is true, show "Nueva Tarea" button when false
- [x] T048 [US3] Add visual feedback in TaskItem when task is selected (highlighting or background change)
- [x] T049 [US3] Implement selection persistence logic: maintain selection when switching tabs if selected tasks are visible in new tab, clear if not visible

**Checkpoint**: At this point, User Stories 1, 2, AND 3 should work independently. Users can view tasks, create new ones, and select multiple tasks with selection mode.

---

## Phase 6: User Story 4 - Completar Tareas Seleccionadas (Priority: P2)

**Goal**: El usuario puede completar una o múltiples tareas seleccionadas mediante el botón "Completar" en la barra de acciones del modo selección.

**Independent Test**: Seleccionar una o más tareas pendientes, hacer clic en "Completar" y verificar que las tareas se mueven al estado Completadas y desaparecen de la pestaña actual.

### Implementation for User Story 4

- [x] T050 [US4] Implement TaskRepository.mutations() update method in `src/app/features/tasks/repositories/task-repository.ts` using PATCH /tasks/:id/status endpoint
- [x] T051 [US4] Implement TaskManager.completeSelected() method in `src/app/features/tasks/managers/task-manager.ts` to update multiple tasks to 'completed' status
- [x] T052 [US4] Add "Completar" button handler in TaskList component calling TaskManager.completeSelected() with selectedTasks array
- [x] T053 [US4] Implement batch status update logic: iterate selectedTasks and call repository update for each task with status 'completed'
- [x] T054 [US4] Clear selectedTasks signal after successful completion operation
- [x] T055 [US4] Reload tasks list after completion to reflect status changes (tasks disappear from current tab, appear in Completadas tab)
- [x] T056 [US4] Handle edge case: ignore tasks that are already completed when batch completing
- [x] T057 [US4] Deactivate selection mode after completion operation completes

**Checkpoint**: At this point, User Stories 1-4 should work independently. Users can view, create, select, and complete tasks.

---

## Phase 7: User Story 5 - Eliminar Tareas Seleccionadas (Priority: P2)

**Goal**: El usuario puede eliminar una o múltiples tareas seleccionadas mediante el botón "Eliminar" en la barra de acciones del modo selección.

**Independent Test**: Seleccionar una o más tareas, hacer clic en "Eliminar", confirmar la acción y verificar que las tareas desaparecen del listado.

### Implementation for User Story 5

- [x] T058 [US5] Implement TaskRepository.mutations() delete method in `src/app/features/tasks/repositories/task-repository.ts` using DELETE /tasks/:id endpoint
- [x] T059 [US5] Implement TaskManager.deleteSelected() method in `src/app/features/tasks/managers/task-manager.ts` with confirmation dialog following EntityManager.delete() pattern
- [x] T060 [US5] Add "Eliminar" button handler in TaskList component calling TaskManager.deleteSelected() with selectedTasks array
- [x] T061 [US5] Implement confirmation dialog in TaskManager.deleteSelected() using MessageService.show() with modal type, warning icon, and Cancel/Accept actions
- [x] T062 [US5] Implement batch delete logic: iterate selectedTasks and call repository delete for each task only after user confirms
- [x] T063 [US5] Clear selectedTasks signal after successful deletion operation
- [x] T064 [US5] Reload tasks list after deletion to reflect removed tasks
- [x] T065 [US5] Handle cancellation: maintain selection if user cancels deletion dialog
- [x] T066 [US5] Deactivate selection mode after deletion operation completes
- [x] T067 [US5] Show success message after successful deletion using MessageService

**Checkpoint**: At this point, all User Stories 1-5 should work independently. Users have full CRUD functionality with bulk operations.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [ ] T068 [P] Add i18n markers to all user-facing strings in TaskList, TaskItem, and TaskForm components
- [ ] T069 [P] Add i18n markers to TaskManager messages and dialogs
- [ ] T070 Verify all components follow OnPush change detection strategy
- [ ] T071 Verify all components use inject() for dependency injection
- [ ] T072 Verify all components use signals for reactive state management
- [ ] T073 [P] Add error handling for network failures in TaskRepository methods with appropriate user messages
- [ ] T074 [P] Add loading states for mutation operations (create, update, delete) in TaskManager
- [ ] T075 Verify TaskRepository follows ADR-006 Repository Pattern correctly
- [ ] T076 Verify TaskManager follows EntityManager pattern from templates feature
- [ ] T077 Verify all components follow ADR-002 Angular Style Guide (standalone, OnPush, signals)
- [ ] T078 Verify all styles follow ADR-003 CSS Strategy (Tailwind CSS, ft- prefix for custom)
- [ ] T079 Verify wireframes compliance: tasks-list.png and tasks-list-selection.png layouts match implementation
- [ ] T080 [P] Update documentation: ensure all ADRs are referenced correctly in code comments
- [ ] T081 Run quickstart.md validation checklist
- [ ] T082 Verify performance: list loads in <2 seconds, bulk operations (10 tasks) in <3 seconds

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories
- **User Stories (Phase 3+)**: All depend on Foundational phase completion
  - User stories can then proceed in parallel (if staffed)
  - Or sequentially in priority order (P1 → P2)
- **Polish (Final Phase)**: Depends on all desired user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) - No dependencies on other stories
- **User Story 2 (P1)**: Can start after Foundational (Phase 2) - Depends on US1 for TaskList component structure
- **User Story 3 (P2)**: Can start after Foundational (Phase 2) - Depends on US1 for TaskList and TaskItem components
- **User Story 4 (P2)**: Can start after Foundational (Phase 2) - Depends on US3 for selection functionality
- **User Story 5 (P2)**: Can start after Foundational (Phase 2) - Depends on US3 for selection functionality

### Within Each User Story

- Models before services/repositories
- Repository methods before components that use them
- Components before integration
- Story complete before moving to next priority

### Parallel Opportunities

- All Setup tasks marked [P] can run in parallel
- All Foundational tasks marked [P] can run in parallel (within Phase 2)
- Once Foundational phase completes, user stories can start sequentially (P1 stories first, then P2)
- Within US1: TaskItem component creation (T016-T018) can run in parallel
- Within US2: TaskForm component creation (T033-T035) can run in parallel
- Polish phase tasks marked [P] can run in parallel

---

## Parallel Example: User Story 1

```bash
# Launch all parallel tasks for User Story 1 together:
Task: "Create TaskItem component in src/app/features/tasks/components/task-item/task-item.ts"
Task: "Create TaskItem template in src/app/features/tasks/components/task-item/task-item.html"
Task: "Create TaskItem styles in src/app/features/tasks/components/task-item/task-item.css"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL - blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test User Story 1 independently - navigate tabs, verify tasks display with temporal info
5. Deploy/demo if ready

### Incremental Delivery

1. Complete Setup + Foundational → Foundation ready
2. Add User Story 1 → Test independently → Deploy/Demo (MVP!)
3. Add User Story 2 → Test independently → Deploy/Demo (Full CRUD)
4. Add User Story 3 → Test independently → Deploy/Demo (Selection mode)
5. Add User Story 4 → Test independently → Deploy/Demo (Bulk complete)
6. Add User Story 5 → Test independently → Deploy/Demo (Bulk delete)
7. Each story adds value without breaking previous stories

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together
2. Once Foundational is done:
   - Developer A: User Story 1 (MVP)
   - Developer B: Prepares User Story 2 (waits for US1)
3. After US1 completes:
   - Developer A: User Story 2
   - Developer B: User Story 3
4. After US2 and US3 complete:
   - Developer A: User Story 4
   - Developer B: User Story 5
5. Stories complete and integrate independently

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- Each user story should be independently completable and testable
- Commit after each task or logical group
- Stop at any checkpoint to validate story independently
- Avoid: vague tasks, same file conflicts, cross-story dependencies that break independence
- All tasks follow Angular Base Project conventions (ADR-001, ADR-002, ADR-003, ADR-006)
- MockHttpClient is used throughout development; replace with HttpClient when backend is ready

---

## Summary

- **Total Tasks**: 82
- **Tasks per User Story**:
  - US1 (P1): 16 tasks
  - US2 (P1): 11 tasks
  - US3 (P2): 9 tasks
  - US4 (P2): 8 tasks
  - US5 (P2): 10 tasks
  - Setup: 5 tasks
  - Foundational: 8 tasks
  - Polish: 15 tasks
- **Parallel Opportunities**: 25 tasks marked [P]
- **MVP Scope**: Phases 1-3 (Setup + Foundational + US1) = 29 tasks
- **Independent Test Criteria**: Each user story has clear test criteria for independent validation
