import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'application.base.angular',
  appName: 'angular-base-project',
  webDir: 'undefined'
};

export default config;
